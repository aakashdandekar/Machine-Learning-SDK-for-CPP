[build-system]
requires = ["setuptools>=61.0", "pybind11>=2.12.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ModelTraining"
version = "0.1.0"
description = "Model Training SDK"
requires-python = ">=3.8"
